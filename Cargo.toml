[package]
name = "pal"
version = "0.1.0"
edition = "2021"
description = "PAL: bounded local prototype of an agent-first durable language runtime"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
